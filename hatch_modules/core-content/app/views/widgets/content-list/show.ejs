<% if (widget.settings.title) { %>
    <h2><%- widget.settings.title %></h2>
<% } %>

<% posts.forEach(function (post) { %>
	<%- renderContent(post) %>
<% }) %>

<% if (posts.length === 0) { %>
	<p>
		No posts to display
	</p>
<% } %>